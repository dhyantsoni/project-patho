# public/images/

Put photos and poster images here. Reference them from content files by filename
(e.g. `image: kids-workshop.webp`). Prefer `.webp`/`.avif`; `.jpg`/`.png` are fine.

Remember to add a credit line to `IMAGE-CREDITS.md` for every image you add.
